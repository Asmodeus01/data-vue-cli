#!/usr/bin/env node
const Template = require('../utils/Template')

const template = new Template()
const list = Object.keys(template.getNameList())

for (const item of list) {
  console.log('  ' + item);
}
